@air 0.2

workflow W(content: Message, history: Message[], r1: Message) -> Artifact:
    node two_args:
        result = llm(summarize, content)
        return Artifact(summary=result)

    node list_arg:
        r = llm(gpt, [history, r1])
        return Artifact(data=r)
